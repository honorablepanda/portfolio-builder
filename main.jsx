import React from "react";
import ReactDOM from "react-dom/client";
import PortfolioBuilder from "./index.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PortfolioBuilder />
  </React.StrictMode>
);
